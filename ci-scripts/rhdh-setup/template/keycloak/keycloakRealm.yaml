apiVersion: keycloak.org/v1alpha1
kind: KeycloakRealm
metadata:
  name: backstage
  labels:
    app: keycloak
spec:
  realm:
    id: "backstage"
    realm: "backstage"
    enabled: True
    displayName: "Backstage Realm"
  instanceSelector:
    matchLabels:
      app: keycloak
