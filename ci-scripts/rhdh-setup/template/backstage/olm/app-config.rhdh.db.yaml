backend:
  database:
    connection:
      host: "${POSTGRES_HOST}"
      port: "${POSTGRES_PORT}"
      user: "${POSTGRES_USER}"
      password: "${POSTGRES_PASSWORD}"
    knexConfig:
      pool:
        min: 10
        max: 50
        acquireTimeoutMillis: 60000
