# Page ${PAGE_N} dynamic plugin
- package: oci://quay.io/rhdh-community/rhdh-loadtest-plugins:bs_${DYNAMIC_PLUGIN_BS_VERSION}_page-${PAGE_N}!internal-backstage-plugin-page-${PAGE_N}
  disabled: false
  pluginConfig:
    dynamicPlugins:
      frontend:
        internal.backstage-plugin-page-${PAGE_N}:
          dynamicRoutes:
            - path: /page-${PAGE_N}
              importName: Page
              menuItem:
                icon: dashboard
                text: Page ${PAGE_N}
